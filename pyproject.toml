[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "a3i_general_probe"
version = "0.1.0"
description = "Code for doing linear probing"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "setuptools>=78.1.0",
]
